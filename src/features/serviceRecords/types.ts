export type ServiceRecord = {
  publicId: string;
  productId: string;
  serviceDate: string;
  description: string;
};
export type ServiceRecordInput = {
  serviceDate: string;
  description: string;
  productId: string;
};
